pub struct GameState {
    /* the different game states possible to match with later
       0 = Title Screen
       1 = Gameplay
       2 = Game Over
       3 = You Win!
    */
    pub state: usize,
}
